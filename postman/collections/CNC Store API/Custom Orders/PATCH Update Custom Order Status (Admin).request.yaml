$kind: http-request
name: 'PATCH Update Custom Order Status (Admin)'
method: PATCH
url: '{{baseUrl}}/api/custom-orders/{{customOrderId}}/status'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{adminToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "status": "UNDER_REVIEW"
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function () {
        pm.response.to.have.status(200);
      });
      pm.test("Response has success: true", function () {
        const json = pm.response.json();
        pm.expect(json.success).to.be.true;
      });
      pm.test("Response data.status equals UNDER_REVIEW", function () {
        const json = pm.response.json();
        pm.expect(json.data.status).to.eql("UNDER_REVIEW");
      });
