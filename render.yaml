services:
  - type: web
    name: habib-and-sons-backend
    runtime: node
    rootDir: backend
    plan: starter
    buildCommand: npm ci && npm run build
    preDeployCommand: npm run prisma:deploy
    startCommand: npm run start
    healthCheckPath: /health
    autoDeployTrigger: commit
